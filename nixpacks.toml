# Nixpacks configuration for Railway deployment

[phases.setup]
nixPkgs = ['nodejs_20', 'bun']

[phases.install]
cmds = ['bun install']

[phases.build]
cmds = [
  'bun run build',
  'npx prisma generate'
]

[start]
cmd = 'bun run start'

[variables]
NODE_ENV = 'production'
